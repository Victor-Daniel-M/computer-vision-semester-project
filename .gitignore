# Ignore everything by default so the submitted repository stays focused.
*

# Keep Git metadata helpers.
!.gitignore

# Keep the runnable notebook and dependency list.
!poc_review.ipynb
!requirements.txt

# Ignore common local/cache artifacts even inside allowed folders.
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
